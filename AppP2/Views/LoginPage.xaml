<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppP2.Views.LoginPage"
             Title="Inicio de Sesión"
             BackgroundColor="#F0F4F8">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

            <Label Text="ProfeView" FontSize="32" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#2C3E50"/>
            <Label Text="Califica a tus profesores" FontSize="16" HorizontalOptions="Center" TextColor="#7F8C8D"/>

            <!-- Eliminado el toolkit:CharactersValidationBehavior -->
            <Entry x:Name="EmailEntry" Placeholder="Correo electrónico" Keyboard="Email" FontSize="16"/>

            <Entry x:Name="PasswordEntry" Placeholder="Contraseña" IsPassword="True" FontSize="16"/>

            <Button x:Name="LoginButton" 
                    Text="Iniciar Sesión" 
                    BackgroundColor="#3498DB" 
                    TextColor="White" 
                    CornerRadius="10" 
                    HeightRequest="50" 
                    Clicked="OnLoginClicked"/>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Spacing="5">
                <Label Text="¿No tienes cuenta?" TextColor="#7F8C8D"/>
                <Label Text="Regístrate" TextColor="#3498DB" FontAttributes="Bold">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnRegisterTapped"/>
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>

            <Label x:Name="MessageLabel" IsVisible="False" TextColor="Red" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>